Drop real project thumbnails here when they are ready.

Suggested filenames:
- currency-denomination-converter.png
- fake-news-detection-using-ai.png

After adding a thumbnail, set the matching project's `thumbnail` field in `data/portfolio.ts`.
